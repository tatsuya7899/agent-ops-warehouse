# Internal governance / planning documents.
# This repository will go public (P1); internal docs stay local until
# their public equivalents (English README, etc.) replace them.
SPEC-*.md
CHECKLIST.md

# Generated artifacts
out/
*.ndjson

# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
venv/
.venv/

# Terraform secrets (introduced in P1; ignored ahead of time)
terraform.tfvars
.terraform/

# dbt (P2) -- profiles.yml is committed intentionally, it holds no secrets
# (auth is ADC, project id is env_var-injected). Everything below is a
# regenerable build artifact.
dbt/target/
dbt/logs/
dbt/dbt_packages/
dbt/.user.yml

# OS
.DS_Store
GOVERNANCE-POINTER.md

# dbt run logs, if dbt is ever invoked from the repo root instead of dbt/
# (dbt's own dbt/logs/ above only covers invocations from inside dbt/)
logs/
